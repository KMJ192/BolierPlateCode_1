import React, { Component } from 'react';
import './SideBar.css';

export class SideBar extends Component {
    render() {
        return (
            <div id="side-bar">
                SideBar
            </div>
        )
    }
}

export default SideBar
